\usepackage[semibold]{sourcesanspro}
\usepackage{sectsty}
\usepackage{parskip}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{threeparttable}
\graphicspath{{../figures/}}
\allsectionsfont{\sffamily}
\pagenumbering{gobble}
\captionsetup[figure]{labelfont = {bf}, name = {Figure}, labelsep = space}
\captionsetup[table]{textfont = it, format = plain, justification = justified, singlelinecheck = false, labelsep = space, skip=0pt}
